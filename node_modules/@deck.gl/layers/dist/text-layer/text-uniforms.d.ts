import type { Viewport } from '@deck.gl/core';
export type ContentAlignModes = keyof typeof CONTENT_ALIGN;
declare const CONTENT_ALIGN: {
    readonly none: 0;
    readonly start: 1;
    readonly center: 2;
    readonly end: 3;
};
export type TextModuleProps = {
    contentCutoffPixels?: [number, number];
    contentAlignHorizontal?: ContentAlignModes;
    contentAlignVertical?: ContentAlignModes;
    fontSize: number;
    viewport: Viewport;
};
export declare const textUniforms: {
    readonly name: "text";
    readonly vs: string;
    readonly getUniforms: ({ contentCutoffPixels, contentAlignHorizontal, contentAlignVertical, fontSize, viewport }: Partial<TextModuleProps>) => {
        cutoffPixels: [number, number];
        align: [0 | 2 | 3 | 1, 0 | 2 | 3 | 1];
        fontSize: number | undefined;
        flipY: boolean;
    };
    readonly uniformTypes: {
        readonly cutoffPixels: "vec2<f32>";
        readonly align: "vec2<i32>";
        readonly fontSize: "f32";
        readonly flipY: "f32";
    };
};
export {};
//# sourceMappingURL=text-uniforms.d.ts.map